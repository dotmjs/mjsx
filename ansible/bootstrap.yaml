---
- name: Prep bootstrap system
  hosts: bootstrap
  remote_user: root

  tasks:
  - name: Ensure supporting packages are installed, and at latest version
    ansible.builtin.dnf:
      name:
        - libvirt-daemon-kvm
        - ansible-core
        - git
      state: latest